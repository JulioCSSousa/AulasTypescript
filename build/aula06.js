"use strict";
//declarações array
let numeroReadOnly = [1, 5, 6, 8, 9, "s", "f", "h"];
let numeros = [];
let numerosDeclare = [];
//metodos
numeros.push(5, 3, 4, 8); //no fim
numeros.unshift(); //no começo
numeros.pop(); //tira do fim
numeros.shift(); //tira do inicio
console.log(numeroReadOnly);
